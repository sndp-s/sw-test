import React from 'react';
const CurrencyContext = React.createContext();

// const defaultValue = null;
// export const CurrencyContext = React.createContext(defaultValue);

export default CurrencyContext;